{
  "name": "snaptix",
  "version": "1.0.0",
  "private": true,
  "description": "SnapTix: Production-Quality Real-Time Concurrency-Safe Seat Booking Platform",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "load-test": "node load-test/test.js",
    "install:all": "npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
